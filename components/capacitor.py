
from Logger import Logger
from components import Component

class Capacitor(Component):
    _attributes = {
        
    }

    UNIT = "F"           # Unit of the component (e.g., Ohm, Farad)
    schematic_symbol = None  # Schematic symbol of the component
    NAME = "Capacitor"

